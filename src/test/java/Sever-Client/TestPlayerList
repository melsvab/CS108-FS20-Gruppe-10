import org.junit.Assert;
import org.junit.Test;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.util.LinkedList;
import server.Profil;
import server.Server;

public class TestPlayerList {
/*
    public static final Logger logger = LoggerFactory.getLogger(TestPlayerList.class);

    @Test
    public void checkPlayerList() {
        LinkedList<String> namesOfAllClients = new LinkedList<>();
        Profil client1 = new Profil(1);
        Profil client2 = new Profil(2);
        Profil client3 = new Profil(3);
        client1.nickname = "rohail";
        client2.nickname = "namel";
        client3.nickname = "dennis";


        Assert.assertEquals();

    }*/
}